import LoadingSpinner from '@/app/_components/loading-spinner';

export default function Loading() {
    // Use the regular orange spinner for all pages except homepage
    return <LoadingSpinner />;
}