export interface DetailedTopic {
    id: string,
    name: string,
    indication?: string,
    purpose?: string,
    filter_id?: string,
    description?: string,
    image_url?: string,
    topic_order: number,
    topic_id: string,
    is_private: boolean,
}